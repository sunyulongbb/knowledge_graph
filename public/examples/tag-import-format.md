# 标签导入格式

文件必须是 UTF-8 编码的 JSON，最大 1 MB。

根对象只允许以下字段：

- `version`：固定为 `1`。
- `tags`：非空字符串数组，一次最多 1000 个标签，每个标签最多 100 个字符。

标签会去除首尾空格，并按不区分大小写的方式去重。标签管理中已选中分类时，导入标签会合并到该分类；未选中分类时，会合并到当前应用的全局自定义标签。现有标签不会被删除。
