version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      # 持久化 SQLite 数据库文件 (挂载目录而不是文件)
      - ./data:/app/data
    environment:
      - PORT=3000
    restart: always
